# PR Archaeologist

A CLI agent that builds and maintains a living memory of a repo's decision
history not just what the code does, but why it got that way.

## Status

Early development. Building incrementally.

## Development

```bash
uv sync
uv run pr-arch version
```